checkoutForm = document.querySelector(".checkout-form");

checkoutForm.addEventListener("submit", handleCheckoutNotif);

function handleCheckoutNotif() {
  alert("Thank you so much for choosing us, your order is being processed!");
}
